# PHASE-3 — 阶段三：前端设计与报告页素材

> 本阶段为报告设计视觉方案并产出素材。基于阶段二验证通过的信息，规划整体设计、配色、排版与动效，产出高品质报告页素材。

## 1. 设计与适配要求

- **不限制报告模板风格**，每次报告可以采用不同的设计主题和布局结构
- **每次报告必须具有新意**，可以尝试不同的配色方案、排版方式、动画效果和交互元素
- **禁止参考或复用仓库中已有的历史报告文件（如历史 `index.html`、素材图片等）**：本次迭代是全新独立的生成任务，与上次迭代无关。不得读取、模仿、复用旧报告的代码、结构、文案或设计，必须从零开始重新设计和编写，以确保每次都呈现全新内容
- 网页要有**品位、高级感**，追求高品质的前端呈现
- **必须阅读环境中的插件 / skill**：报告生成前，**必须**主动检查并阅读环境中可用的相关插件、skill，据此指导配色、排版、组件、交互与视觉素材的设计与实现，禁止跳过或凭感觉随意设计。**本仓库 `skills/` 目录内置了两套技能库，是优先使用的一级设计依据：**
  - `skills/ui-ux-pro-max/`：UI/UX 设计情报库（对应 skill 入口 `ui-ux-pro-max/SKILL.md`，含 79 种可搜索 UI 样式、192 配色与产品推理、74 字体搭配、119 条 UX 指南、25 图表类型、22 技术栈与设计系统生成器）。用于整体设计方向、配色、排版、组件、交互与可访问性决策，产出有品位、有依据的设计，而非凭感觉设计。其中 `ui-styling`、`design`、`design-system`、`slides`、`brand`、`banner-design` 为配套设计 skill，按需使用
  - `skills/gsap-skills/`：GSAP 动画技能库（8 个子 skill：`gsap-core`、`gsap-timeline`、`gsap-scrolltrigger`、`gsap-plugins`、`gsap-utils`、`gsap-performance`、`gsap-react`、`gsap-frameworks`）。用于实现高品质动效，尤其当图片/视频素材不可用而触发降级策略（见下条）时，用 GSAP 做出有品位、流畅的动画（滚动触发、时间线编排、粒子、视差、交互等），弥补没有图片/视频的遗憾
  - 除上述内置技能库外，环境中若还有其他可用的设计类插件 / skill，也应一并阅读使用；若环境确无可用的此类插件 / skill，则凭前端设计最佳实践自主设计
- **视频/图片生成（环境中可用时）一定使用**：若环境提供可用的图片生成模型和视频生成模型，本任务**必须**使用它们来辅助设计，产出高品质的**报告页视觉素材**（插画、装饰、动态效果等），不得跳过或省略
- **报告中必须有图片/视频素材（强制要求，不存在豁免情形）**：生成的 HTML 报告中**必须**包含至少一个由图片/视频生成模型产出的真实图片或视频素材，并正常内嵌展示。**降级策略（仅当模型不可用或产出失败时触发）：** 严禁用 emoji、SVG 图形等简单元素充当视觉素材凑数；必须改用高质量的动态效果（CSS/JavaScript/canvas 动画、粒子效果、视差滚动、交互动画等）在视觉与交互上弥补没有图片/视频的遗憾，确保报告依然呈现高品位、高级感的视觉效果
- 可充分利用环境中可用的图片生成模型来辅助设计，做出高品质的前端页面
- **报告页视觉素材**：报告（`index.html`）内嵌使用的插画、图片、视频、装饰等视觉内容，由图片/视频生成模型单独产出，是独立的素材文件
- **所有静态素材统一放入静态资源文件夹 `assets/`**：本次任务产生的**报告页素材**（插画、图片、视频等）一律集中存放在 `assets/` 目录下，不得散落堆放在仓库根目录；`index.html` 中通过相对路径（如 `assets/xxx.png`）引用这些素材
- **强制要求完美的移动端适配：**
  - 使用响应式设计，适配 320px 至 1920px 所有常见屏幕尺寸
  - 移动端菜单采用汉堡菜单设计
  - 字体大小、按钮尺寸、间距在移动端自动调整
  - 确保所有交互元素在触屏设备上易于操作
  - 图片自动缩放，避免横向滚动条
- **完成后必须进行视觉检查，特别注意移动端效果**

## 完成后

- 在 `CHECKLIST.md` 的阶段三打勾（`- [x]`）
- 重新阅读 `AGENT.md` 总纲与 `CHECKLIST.md`，确认阶段四需求
- **读取 `PHASE-4.md`，开始阶段四**