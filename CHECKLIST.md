# 阶段工作清单（CHECKLIST）

> 本文件是 Saya 每次任务的执行进度清单（对应 `AGENT.md` 第十章）。任务启动、完成每阶段、以及任务收尾时都必须打开并操作本文件。

## 10.1 阶段清单（每完成一阶段，勾选该阶段）

- [ ] **阶段一 · 初始化与阅读**：拉取仓库最新代码，通读 `AGENT.md`（重点核对第九章配置与本章清单），明确本次全部需求。**本次任务全程禁止向用户提出任何问题**，所有决定必须自主做出
- [ ] **阶段二 · 多 agent 并行信息检索与验证**：按 AGENT.md 2.4 启用多 agent 并行，对五大板块执行 2.1 多轮检索、2.3 多样搜索（含尝试不同搜索工具）、2.2 多渠道验证，达到第五章质量规范
- [ ] **阶段三 · 前端设计与报告页素材**：按 AGENT.md 3.2/9.3，若环境中存在可用的前端设计文档 / 设计 skill 则必须加载阅读；**报告中必须有图片/视频素材（强制要求）**，若环境提供图片/视频生成模型则必须使用并产出**报告页素材**（插画、图片、视频等，供 `index.html` 内嵌使用），**统一放入 `assets/` 静态资源文件夹**；**仅当模型不可用或产出失败时**触发降级：严禁用 emoji/SVG 凑数，改用高质量动画（CSS/JS/canvas、粒子、视差、交互动画等）弥补
- [ ] **阶段四 · 生成 HTML 报告**：基于验证后的信息与素材，生成完整 `index.html`，符合 AGENT.md 第三章全部规范，完成移动端适配与视觉检查
- [ ] **阶段五 · 部署与更新**：按 AGENT.md 9.1/9.2 完成 Git 配置、pull 同步、提交推送。**README 头图处理**（按 AGENT.md 9.2 规则）：① 用图片生成模型生成 README 头图 `assets/banner.png`（含 Saya 猫娘形象、非主要位置标注本次日期）；② 生成失败或未产出可用头图时，用 `assets/banner-default.png` 复制覆盖 `assets/banner.png`；③ **严禁复用上一轮生成的头图**；④ **除头图 `assets/banner.png` 外，不得更改 `README.md` 的任何其他内容**。推送内容含 `index.html`、本次报告页素材、README 头图 `banner.png`，验证 Pages 部署成功

## 10.2 打勾推进规则

- **首次阅读本文件时，若发现阶段清单中有任何打勾（`- [x]`），必须先将其全部取消（恢复为 `- [ ]`），再开始本次任务**——上一轮任务遗留的勾选与本次无关，必须清空重来
- **每完成一个阶段，立即在该阶段前打勾（`- [x]`），然后重新阅读 `AGENT.md` 与 `CHECKLIST.md`**，确认下一阶段的具体需求与规范，再开始执行下一阶段
- **发生上下文压缩后，必须重新阅读 `AGENT.md` 与 `CHECKLIST.md` 全文**，确认尚未完成的目标、当前所处阶段与剩余任务，再继续执行，不得凭压缩前的记忆直接推进
- 任何阶段未达到对应章节的质量要求，**不得打勾**，必须补齐后再进入下一阶段；**已达标阶段保留其勾选**，不得因后续阶段受阻而清除
- **任务因环境 / 外部限制而提前终止时（如阶段五部署因环境变量缺失无法完成）**：保留已完成阶段的勾选状态，未达标阶段保持 `- [ ]`，**不执行收尾清空**；勾选状态随当前进度一并提交到仓库，便于下次任务识别从何处继续
- **全部五个阶段均达标完成后**，取消所有打勾（将清单恢复为全部 `- [ ]` 空状态），作为本次任务正常完成的收尾动作，供下次任务重新勾选

> 清单状态随报告一起提交到仓库，便于追踪每次任务是否完整走完全部阶段。