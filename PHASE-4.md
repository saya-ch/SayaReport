# PHASE-4 — 阶段四：生成 HTML 报告 + 独立复审

> 本阶段基于阶段二验证通过的信息与阶段三产出的素材，由主 agent 亲自生成完整 HTML 报告，并启用独立 subagent 复审。

## 1. 报告生成责任

- HTML 报告（`index.html`）**必须由主 agent（Saya）亲自生成**，禁止委托 subagent 代写报告；**subagent 仅限用于本阶段的报告复审环节**（见本节「5. 报告复审要求」），不得用于检索、设计、生成等其他任何环节
- **报告生成前，必须先阅读环境中可用的插件 / skill**（见 `PHASE-3.md` 1.设计与适配要求，重点优先使用 `skills/guizang-ppt-skill/`），再开始生成；生成完成后执行完整的视觉检查

## 2. 输出格式要求

- **必须输出完整的 HTML 网页文件**（`index.html`），包含所有必要的 CSS 样式和 JavaScript 交互代码
- **报告只呈现资讯内容本身**：任务执行过程中遇到的所有阻碍、异常或环境限制（如图片/视频生成不可用、工具受限、部署失败等），**一律不得写入报告中**，直接跳过或按规范自主处理，报告页不出现任何任务执行说明
- 禁止输出 Markdown 格式内容，所有报告内容必须嵌入 HTML 结构中
- HTML 代码必须符合 W3C 标准，语义化标签使用正确
- 代码结构清晰，添加必要的注释，便于后续维护和修改

## 3. 页面功能要求

- 所有外部链接必须在新标签页中打开
- 在报告底部显著位置显示：「最后更新时间：YYYY-MM-DD HH:MM」
- 在报告底部显示 GitHub Pages 访问链接和本次提交记录链接
- **每条信息必须包含可点击的超链接**：信息条目的标题 / 名称本身可点击，跳转到对应来源页面；同时每条信息末尾标注信息来源链接，格式为「信息来源：[官方网站名称](官方链接)」。禁止出现无任何超链接的孤立信息条目

## 4. 助手语气落地（页眉、页脚与欢迎语）

- 在 HTML 报告的**页眉、页脚和欢迎语**中融入 Saya 的猫娘形象和语气（见总纲「五、助手身份与语气规范」）

## 5. 报告复审要求（HTML 生成完成后必须执行）

- **HTML 报告生成完成后，必须启用一个独立的 subagent 对报告进行复审**；复审者与报告生成者分离，独立检查，不得由生成者自我确认
- 复审必须同时关注两方面质量：
  - **内容质量**：各板块信息是否完整、准确、来源可核验；**每条信息是否都有可点击的超链接**；是否有过期、待核实或来源不明的信息未按要求标注；时效窗口与「最后更新时间」是否正确
  - **呈现质量**：整体设计是否有品位、高级感；移动端（320px）是否无横向溢出、交互是否易操作；图片/视频素材或降级动画是否到位；外部链接是否新标签页打开；报告是否包含任务执行说明等禁止内容（见本节「2. 输出格式要求」）
- 复审发现问题 → 修复后再次复审，直至全部通过
- **复审通过后才可进入阶段五部署**

## 6. 额外加分项（仅在完成所有核心任务后考虑）

**只有在以下所有条件都满足的前提下，才可以考虑添加以下功能：**

1. 信息检索与验证工作 100% 完成且质量达标
2. 所有五个信息板块内容完整、准确、来源可靠
3. HTML 报告基本功能全部实现
4. 部署工作顺利完成

- 可以添加数据可视化图表，展示行业趋势、薪资水平等信息
- 可以实现简单的搜索功能，方便用户快速查找感兴趣的内容
- 可以添加订阅功能，允许用户通过邮件接收最新报告
- 可以添加分享功能，方便用户将有用的信息分享给同学
- 可以根据用户的反馈不断优化报告内容和设计风格

## 完成后

- 在 `CHECKLIST.md` 的阶段四打勾（`- [x]`）
- 重新阅读 `AGENT.md` 总纲与 `CHECKLIST.md`，确认阶段五需求
- **读取 `PHASE-5.md`，开始阶段五**